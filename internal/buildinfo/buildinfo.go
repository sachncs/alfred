// Package buildinfo carries the binary version string shared between
// the CLI entrypoint and the runtime configuration.
package buildinfo

// Version is the Alfred release identifier. Bump this alongside CHANGELOG.md.
const Version = "0.7.0-phase7"
