# remote_executor

`mcp.remote_executor` — run jobs on remote targets.

## Tools

- `remote_target_list` — list configured remote targets
- `remote_run` — submit a job to a remote target
- `remote_job_status` — query the status of a remote job
