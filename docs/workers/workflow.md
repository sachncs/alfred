# workflow

`mcp.workflow` — workflow engine.

## Purpose

Define and execute multi-step workflows that combine multiple workers.

## Tools

- `workflow_list` — list defined workflows
- `workflow_run` — execute a workflow
- `workflow_status` — query a running workflow's status
