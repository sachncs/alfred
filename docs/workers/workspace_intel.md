# workspace_intel

`mcp.workspace_intel` — workspace intel preview.

## Tools

- `workspace_list` — list workspace artifacts
- `workspace_read` — read an artifact
- `workspace_visual_inspect` — run a visual inspection on an artifact
