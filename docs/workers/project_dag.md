# project_dag

`mcp.project_dag` — project planning DAG.

## Purpose

Track project tasks with dependencies, similar to evidence_dag but for
implementation work.

## Tools

- `project_update` — update a task in the DAG
- `project_graph` — read the project DAG
- `project_review` — surface blockers and ready tasks
