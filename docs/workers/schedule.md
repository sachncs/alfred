# schedule

`mcp.schedule` — scheduled task worker.

## Purpose

Create and run scheduled tasks (cron-like).

## Tools

- `schedule_create` — create a new scheduled task
- `schedule_run` — trigger a task immediately
- `schedule_audit` — audit the schedule store
