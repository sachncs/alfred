# ppt_master

`mcp.ppt_master` — PowerPoint deck builder.

## Purpose

Generate `.pptx` decks from a structured outline and validate layout choices.

## Tools

- `ppt_generate` — generate a deck
- `ppt_validate_layout` — validate the layout of an existing deck
