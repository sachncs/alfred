# workspace_bioimaging

`mcp.workspace_bioimaging` — bioimaging workspace preview.

## Tools

- `bioimaging_preview` — preview a bioimaging artifact
- `bioimaging_select_region` — select a region of interest
- `bioimaging_export_roi` — export a region of interest
