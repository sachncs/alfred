# visual_document

`mcp.visual_document` — visual document composer.

## Purpose

Open, edit, and accept visual document changes (markdown + images + tables).

## Tools

- `visual_open` — open a document
- `visual_edit` — apply an edit
- `visual_accept` — accept a pending change
